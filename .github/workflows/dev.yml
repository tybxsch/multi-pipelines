# .github/workflows/dev-new-main.yml
name: Dev New Main Workflow

on:
  workflow_dispatch:  # Permite executar manualmente
    inputs:
      dev-env-name:
        description: 'Dev Environment Name for New Main'
        required: true
      ttl:
        description: Select TTL environment (hours)
        required: true
        type: choice
        default: 'dev1h'
        options:
          - 'dev1h'
          - 'dev3h'
          - 'dev6h'
  push:
    branches:
      - feat/new-main

env:
  APP_NAME: fsd-new-main
  PROJECT_NAME: new-main-project
  SH8: ${{ github.sha }}
  STACK_NAME: "${{ github.event.inputs.ttl }}-${{ github.event.inputs.dev-env-name }}"
  PUBLIC_URL: "https://b${{ github.event.inputs.ttl }}-${{ github.event.inputs.dev-env-name }}.new-main-dev.com/"
  GITHUB_WORKFLOW: "📦🚀 Create Dev Environment for New Main ${{ github.event.inputs.dev-env-name }}"

jobs:
  build:
    runs-on: ubuntu-latest
    environment: development-new-main

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '>=16.7.0'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm run build

      - name: Deploy to Development Environment
        run: |
          echo "Deploying to Development Environment for New Main"
          # Adicione seu script de deploy aqui
